.DS_Store
heapsort_demo
googlestyle.html
lecture1_reveal.html
strassen.html
# Anchored to the repo root: bare patterns match at every directory level,
# which silently excluded codes/styles.css and left the deployed code
# library with no stylesheet at all.
/scripts.js
/styles.css
